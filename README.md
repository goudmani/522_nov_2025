# 522_nov_2025

# This is the new file for group project I guess? IDK man 

New files shall be added to this repo

- `git branch <name>` create a new branch 
- `git switch <name>` switches to new branch
- `git switch -c <name>` create branch and switch to new branch
